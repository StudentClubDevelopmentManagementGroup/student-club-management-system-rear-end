package team.project.module._template.internal.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class TmplConfig {
    /* 这里定义只有本模块会使用到的配置信息（模块内部的全局变量）
      可以使用 yml 配置文件 */
}
