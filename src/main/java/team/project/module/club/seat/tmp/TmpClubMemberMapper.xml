<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.project.module.club.seat.tmp.TmpClubMemberMapper">

    <select id="selectAllMember" resultType="java.lang.String">
        select user_id from tbl_user_club where is_deleted = 0 and club_id = #{clubId}
    </select>

</mapper>