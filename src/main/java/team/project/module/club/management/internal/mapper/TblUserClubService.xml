<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.project.module.club.management.internal.mapper.TblUserClubMapper">
<!--TODO 更改user操作回user模块的接口-->
    <update id="createManager">
        insert into tbl_user_club (role, user_id, club_id)
        values (2, #{userId}, #{clubId})
    </update>

    <update id="setManager">
        update tbl_user_club set role = role | 2 where user_id = #{userId} and club_id = #{clubId}
    </update>

    <update id="quashManager">
        update tbl_user_club set role = role &amp; ~2 where user_id = #{userId} and club_id = #{clubId}
    </update>

    <update id="setUserManager">
        update tbl_user set role = role | 2 where id = #{userId}  and is_deleted = 0
    </update>

    <update id="quashUserManager">
        update tbl_user set role = role &amp; ~2  where id = #{userId} and is_deleted = 0
    </update>
</mapper>
